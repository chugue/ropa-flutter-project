import 'package:flutter/material.dart';

class CustomCirular extends StatelessWidget {
  const CustomCirular({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Center(child: CircularProgressIndicator());
  }
}
