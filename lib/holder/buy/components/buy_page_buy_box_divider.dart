import 'package:flutter/material.dart';

class BuyBoxDivider extends StatelessWidget {
  const BuyBoxDivider({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 10,
      child: Container(
        color: Colors.black12,
      ),
    );
  }
}
